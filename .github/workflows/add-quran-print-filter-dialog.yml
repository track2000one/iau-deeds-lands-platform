name: Add Quran Print Filter Dialog

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'scripts/add-quran-print-filter-dialog.py'
      - '.github/workflows/add-quran-print-filter-dialog.yml'

permissions:
  contents: write

concurrency:
  group: add-quran-print-filter-dialog
  cancel-in-progress: true

jobs:
  apply-build-commit:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Apply print filter upgrade
        run: python3 scripts/add-quran-print-filter-dialog.py

      - name: Install dependencies
        run: npm ci

      - name: Verify frontend build
        run: npm run build

      - name: Commit verified source change
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/app/pages/MosquesUnitPage.tsx
          if git diff --cached --quiet; then
            echo "No frontend source changes to commit"
            exit 0
          fi
          git commit -m "feat(mosques): add Quran print filters and sorting"
          git push origin main
