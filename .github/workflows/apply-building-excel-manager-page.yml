name: Apply Building Excel Manager Page

on:
  push:
    branches: [main]
    paths:
      - 'src/app/components/BuildingExcelImportManager.tsx'
      - 'scripts/apply-building-excel-manager-page.mjs'
      - '.github/workflows/apply-building-excel-manager-page.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: node scripts/apply-building-excel-manager-page.mjs
      - run: npm run build
      - name: Commit changes
        run: |
          if git diff --quiet; then
            echo "No changes"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/app/pages/MosquesUnitPage.tsx
          git commit -m "Integrate reviewed Excel import for building coverage"
          git push
