name: Apply Prayer Room Gender Filter

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/apply-prayer-room-gender-filter.yml'

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: node scripts/apply-prayer-room-gender-filter.mjs
      - run: npm run build
      - name: Remove temporary patch files
        run: rm -f scripts/apply-prayer-room-gender-filter.mjs .github/workflows/apply-prayer-room-gender-filter.yml
      - name: Commit changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/app/pages/MosquesUnitPage.tsx scripts/apply-prayer-room-gender-filter.mjs .github/workflows/apply-prayer-room-gender-filter.yml
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Add prayer room gender filter"
            git push origin main
          fi
