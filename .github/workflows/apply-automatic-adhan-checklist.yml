name: Apply Automatic Adhan Checklist

on:
  push:
    paths:
      - 'scripts/apply-automatic-adhan-checklist.mjs'
      - '.github/workflows/apply-automatic-adhan-checklist.yml'

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - name: Apply automatic adhan checklist UI
        run: node scripts/apply-automatic-adhan-checklist.mjs
      - name: Verify frontend build
        run: npm run build
      - name: Commit implementation and cleanup
        run: |
          rm -f scripts/apply-automatic-adhan-checklist.mjs
          rm -f .github/workflows/apply-automatic-adhan-checklist.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --quiet && exit 0
          git commit -m "Add automatic adhan field visit checklist UI"
          git push
