name: Apply Archive Permission Fixes Frontend

on:
  push:
    branches: [fix/platform-audit-core]
    paths:
      - '.github/workflows/apply-archive-permission-fixes.yml'

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/platform-audit-core
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: node scripts/apply-archive-permission-fixes.mjs
      - run: npm run build
      - name: Remove temporary patch files
        run: rm -f scripts/apply-archive-permission-fixes.mjs .github/workflows/apply-archive-permission-fixes.yml
      - name: Commit verified fixes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/app/pages/ArchivePage.tsx scripts/apply-archive-permission-fixes.mjs .github/workflows/apply-archive-permission-fixes.yml
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Align archive actions with module permissions"
            git push origin fix/platform-audit-core
          fi
