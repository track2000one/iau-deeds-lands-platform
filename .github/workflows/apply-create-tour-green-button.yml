name: Apply Create Tour Green Button

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/apply-create-tour-green-button.yml'

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: node scripts/apply-create-tour-green-button.mjs
      - run: npm run build
      - name: Remove temporary patch files
        run: rm -f scripts/apply-create-tour-green-button.mjs .github/workflows/apply-create-tour-green-button.yml
      - name: Commit changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/app/components/MosqueFieldVisitsPanel.tsx scripts/apply-create-tour-green-button.mjs .github/workflows/apply-create-tour-green-button.yml
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Style create tour button green"
            git push origin main
          fi
