name: Apply Merged Quran Field Census V2

on:
  push:
    branches: [main]
    paths:
      - 'scripts/apply-merged-quran-field-census-v2.mjs'
      - '.github/workflows/apply-merged-quran-field-census-v2.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: node scripts/apply-merged-quran-field-census-v2.mjs
      - run: npm run build
      - name: Commit changes
        run: |
          if git diff --quiet; then
            echo "No changes to commit"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/app/api/mosques.ts src/app/components/MosqueFieldVisitsPanel.tsx
          git commit -m "Merge Quran field checks with visit census sync"
          git push
