name: Apply Accounting Property Evidence Checklist

on:
  push:
    branches:
      - feat/accounting-property-evidence-checklist-v2

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/accounting-property-evidence-checklist-v2
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: node scripts/run-accounting-property-evidence-checklist.mjs
      - run: npm run build
      - name: Remove temporary automation files
        run: |
          rm scripts/apply-accounting-property-evidence-checklist.mjs
          rm scripts/run-accounting-property-evidence-checklist.mjs
          rm .github/workflows/apply-accounting-property-evidence-checklist.yml
      - name: Commit integration changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "4189824+github-actions[bot]@users.noreply.github.com"
          git add -A
          if git diff --cached --quiet; then
            echo "No generated changes"
          else
            git commit -m "Integrate property evidence checklist"
            git push origin HEAD:feat/accounting-property-evidence-checklist-v2
          fi
