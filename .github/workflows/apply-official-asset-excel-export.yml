name: Apply Official Asset Excel Export

on:
  push:
    paths:
      - 'scripts/apply-official-asset-excel-export.mjs'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
      - run: npm ci
      - run: node scripts/apply-official-asset-excel-export.mjs
      - run: npm run build
      - name: Commit changes
        run: |
          if git diff --quiet; then exit 0; fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add package.json package-lock.json src/app/api/assets.ts src/app/pages/AssetReportsPage.tsx
          git commit -m "Add official asset Excel template export workflow"
          git push origin HEAD:main
