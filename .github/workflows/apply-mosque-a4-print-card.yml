name: Apply Mosque A4 Print Card

on:
  push:
    branches: [main]
    paths:
      - 'scripts/apply-mosque-a4-print-card.py'
      - '.github/workflows/apply-mosque-a4-print-card.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  apply-and-verify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Apply patch
        run: python scripts/apply-mosque-a4-print-card.py

      - name: Install dependencies
        run: npm ci

      - name: Verify frontend build
        run: npm run build

      - name: Commit verified source change
        run: |
          if git diff --quiet -- src/app/pages/MosquesUnitPage.tsx; then
            echo "No source changes to commit."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/app/pages/MosquesUnitPage.tsx
          git commit -m "feat(mosques): add printable A4 site card with photo thumbnails"
          git push
